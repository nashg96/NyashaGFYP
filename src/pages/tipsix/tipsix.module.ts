import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { TipsixPage } from './tipsix';

@NgModule({
  declarations: [
    TipsixPage,
  ],
  imports: [
    IonicPageModule.forChild(TipsixPage),
  ],
})
export class TipsixPageModule {}
